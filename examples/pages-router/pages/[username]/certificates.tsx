export default function Certificates() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>This page should never be reached!</h1>
      <p>The middleware should rewrite /username/certificates to /username</p>
    </div>
  );
}
